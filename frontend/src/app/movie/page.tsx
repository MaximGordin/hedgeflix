import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Hedgeflix | Movie',
  description: 'Movie Page',
};

export default function MoviePage() {
  return (
    <div>
      Movie
    </div>
  );
}
